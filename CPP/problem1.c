#include <stdio.h>
#include <math.h>
int foo(A, n);

int main()
{
    m = A[0];
    x = 0;
        for
            i = 0 to n - 1
            {
                x = x + A[i];
                if (m < x)
                {
                    m = x;
                }
                if (x < 0)
                {
                    x = 0;
                }
            }

        return (n);
}